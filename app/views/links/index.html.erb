<p style="color: green"><%= notice %></p>

<h1>Links</h1>

<div id="links">
  <% @links.each do |link| %>
    <p>
      <%= link.title %> - 
      (<%= link_to "View", link %>
      | <%= link_to "Edit", edit_link_path(link) %>)
    </p>
  <% end %>
</div>

<%= link_to "New link", new_link_path %>
