<%= content_for :title, "Edit Tag: #{@tag.name}" %>

<h1>Editing tag: <%= @tag.name %></h1>

<%= render "form", tag: @tag %>

<br>

<div>
  <%= link_to "Show this tag", @tag %> |
  <%= link_to "Back to tags", tags_path %>
</div>
